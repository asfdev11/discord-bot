import '../framework/interfaces/robot.dart';

class DiscordRobot extends IRobot {
  DiscordRobot() {
    initState();
  }

  @override
  void initState() {
    super.initState();
  }

  @override
  void build() {
    super.build();
  }

  @override
  void close() {
    super.close();
  }
}
